@model List<ResultTeamSocialMediaDtos>

<div class="d-flex justify-content-center">
    @foreach(var item in Model)
    {
        <a class="btn btn-square btn-outline-secondary rounded-circle mx-1" href="@item.Url"><i class="@item.Icon"></i></a>
    }
   
</div>