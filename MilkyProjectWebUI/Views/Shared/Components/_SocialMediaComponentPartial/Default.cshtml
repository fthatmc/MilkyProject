@model List<SocialMediaDtos>


<div class="d-flex pt-3">
    @foreach(var x in Model)
    {

        <a class="btn btn-square btn-secondary rounded-circle me-2" href="@x.Url"><i class="@x.Icon"></i></a>
    }

</div>